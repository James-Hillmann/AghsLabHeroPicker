# All server-only. Never prefix with NEXT_PUBLIC_.
SESSION_SECRET=            # signs the session cookie; `node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"`
SITE_PASSWORD_JAMES=       # James's passphrase (identifies his rankings)
SITE_PASSWORD_LIAM=        # Friend's passphrase; must differ from James's
DATABASE_URL=              # Neon Postgres connection string (from the Vercel Neon integration)
